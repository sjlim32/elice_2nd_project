// import { useState } from "react";

function CheckListResult({ setIsSubmit }) {
  const result = "당신의 가정폭력 점수";
  setIsSubmit(false);
  return <> {result} setIsSubmit(false);</>;
}

export default CheckListResult;
